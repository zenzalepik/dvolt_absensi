import 'package:flutter/material.dart';

class WorkPage extends StatelessWidget {
  const WorkPage({super.key});

  @override
  Widget build(BuildContext WorkPage) {
    return Container(child: const Text('WorkPage'));
  }
}
